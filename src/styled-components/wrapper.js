import styled from 'styled-components'

const Wrapper = styled.div`
  display: flex;
  width: 100%;
  gap: 2px;
  height: 100%;

  .col-narrow {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
`
export { Wrapper }
